diff --git a/dist/module.mjs b/dist/module.mjs
index fb002e6278f7fb05cca8f2c27b5de9caddbec101..e03ecb075a68c81db3dc2a94df2877cc85853ef8 100644
--- a/dist/module.mjs
+++ b/dist/module.mjs
@@ -173,11 +173,14 @@ function processUnistPlugins(plugins) {
   Object.entries(plugins).forEach(([name, plugin]) => {
     const instanceName = `_${pascalCase(name).replace(/\W/g, "")}`;
     if (plugin) {
-      imports.push(`import ${instanceName} from '${plugin.src || name}'`);
+      const specifier = plugin.src || name;
+      const isTsPlugin = /\.(?:[cm]?ts|tsx)(?:[?#].*)?$/.test(String(specifier)) && !/\.d\.ts(?:[?#].*)?$/.test(String(specifier));
+      imports.push(isTsPlugin ? `import { unrun as ${instanceName}Loader } from 'unrun'` : `import ${instanceName} from '${specifier}'`);
+      const instance = isTsPlugin ? `(await ${instanceName}Loader({ path: ${JSON.stringify(specifier)} })).module` : instanceName;
       if (Object.keys(plugin).length) {
-        definitions.push(`  '${name}': { instance: ${instanceName}, options: ${JSON.stringify(plugin.options || plugin)} },`);
+        definitions.push(`  '${name}': { instance, options: ${JSON.stringify(plugin.options || plugin)} },`);
       } else {
-        definitions.push(`  '${name}': { instance: ${instanceName} },`);
+        definitions.push(`  '${name}': { instance },`);
       }
     } else {
       definitions.push(`  '${name}': false,`);
diff --git a/dist/runtime/parser/handlers/code.js b/dist/runtime/parser/handlers/code.js
index f87404ebde2bd152e393be9c8474e53ad3629cae..735081241215bf6246946ec7c796c794b02996c1 100644
--- a/dist/runtime/parser/handlers/code.js
+++ b/dist/runtime/parser/handlers/code.js
@@ -1,9 +1,8 @@
-import { detab } from "detab";
 import { parseThematicBlock } from "./utils.js";
 export default (state, node) => {
   const lang = (node.lang || "") + " " + (node.meta || "");
   const { language = "text", highlights, filename, meta } = parseThematicBlock(lang);
-  const value = node.value ? detab(node.value + "\n") : "";
+  const value = node.value ? node.value + "\n" : "";
   let result = {
     type: "element",
     tagName: "code",
diff --git a/dist/runtime/parser/handlers/inlineCode.js b/dist/runtime/parser/handlers/inlineCode.js
index a79488ac2d4b997ee1dcc244db1ea6103a223639..f2847359799aad0d47b8d57396fec97c05dc237b 100644
--- a/dist/runtime/parser/handlers/inlineCode.js
+++ b/dist/runtime/parser/handlers/inlineCode.js
@@ -5,7 +5,7 @@ export default function inlineCode(state, node) {
   const result = {
     type: "element",
     tagName: "code",
-    properties: node.attributes || {},
+    properties: { ...node.attributes, code: text.value },
     children: [text]
   };
   const classes = (result.properties.class || "").split(" ");
